version: '3.1'
services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3309:3309"
    volumes:
      - devdojo_data:/var/lib/mysql

volumes:
  devdojo_data: